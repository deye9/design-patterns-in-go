A builder is a separate component used for building an object.

To make builders fluent, return the receiver - allows for chaining

Different Facets of an object can be build with different builders working in tandem via a common struct.